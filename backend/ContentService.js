var CONTENT_HEADERS = ['Content ID','Deliverable ID','Title','Status','Assigned To','Publish Date'];
function createContentService_(data){if(!data||!data.deliverableId)throw new Error('Deliverable is required.');if(!asText_(data.title))throw new Error('Content title is required.');var item={'Content ID':generateId_(CONFIG.ID_PREFIX.CONTENT),'Deliverable ID':data.deliverableId,'Title':asText_(data.title),'Status':asText_(data.status)||CONFIG.DEFAULTS.DELIVERABLE_STATUS,'Assigned To':asText_(data.assignedTo),'Publish Date':data.publishDate?new Date(data.publishDate):''};appendRow_(APP.SHEETS.CONTENT_BANK,CONTENT_HEADERS,item);recordActivity_('Content',item['Content ID'],'Created content');return item;}
function getContentService_(deliverableId){var s=getCRMSpreadsheet_().getSheetByName(APP.SHEETS.CONTENT_BANK);if(!s||s.getLastRow()<2)return [];var v=s.getDataRange().getValues(),h=v.shift();return v.filter(function(r){return r.some(function(x){return x!=='';})&&(!deliverableId||String(r[h.indexOf('Deliverable ID')])===String(deliverableId));}).map(function(r){var o={};h.forEach(function(k,i){o[k]=r[i]});return o;});}
