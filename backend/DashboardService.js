function getDashboardService_(){var clients=getClientsService_(),projects=getProjectsService_(),payments=getPaymentsService_(),activities=getActivitiesService_();var pending=payments.filter(function(p){return String(p['Status']).toLowerCase()!=='received';}).reduce(function(t,p){return t+Number(p['Amount']||0)},0);return {totalClients:clients.length,activeClients:clients.filter(function(c){return c['Status']==='Active'}).length,totalProjects:projects.length,pendingPayments:pending,recentActivities:activities.slice(-10).reverse()};}
function getActivitiesService_(){var s=getCRMSpreadsheet_().getSheetByName(APP.SHEETS.ACTIVITIES);if(!s||s.getLastRow()<2)return [];var v=s.getDataRange().getValues(),h=v.shift();return v.filter(function(r){return r.some(function(x){return x!=='';});}).map(function(r){var o={};h.forEach(function(k,i){o[k]=r[i] instanceof Date?Utilities.formatDate(r[i],CONFIG.TIMEZONE,'dd-MMM-yyyy HH:mm'):r[i]});return o;});}
